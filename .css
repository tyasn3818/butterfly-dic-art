 * {
    padding: 0;
    margin: 0;
    box-sizing: border-box;
    scroll-behavior: smooth;
    font-family: 'Times New Roman', Times, serif;
    
 }
 body {
    background: linear-gradient(to right, #abdae7, #eea7f16b);
  margin: 0;
  padding: 0;
  color: #333;
 }
 .container {
    width: 100%;
    margin-inline: auto;
    background-color: #dcfffae1;
    position: fixed;
     top: 0;
    left: 0;
    backdrop-filter: blur (8px);
    z-index: 1000;
    padding: 2px 20px;
    height: auto;
 }
 @media (max-width: 992px) {
   .container {
     width: 95%;
   }
 }
 .logo {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px 60px;
  background: transparent;
 }
 .logo h2 {
    font-weight: 50;
    color: #11804e;
 }
 .navbar-box {
    display: flex;
    justify-content: space-between;
    align-items: center;
    top: 0;
    left: 0;
    backdrop-filter: blur (8px);
    z-index: 1000;
    padding: 2px 20px;
    height: auto;
 }
 .navbar-box .menu {
    display: flex;
    gap: 25px;
    justify-content: center;
 }
 .navbar-box .menu li {
    list-style-type: none;
 }
 .navbar-box .menu li a {
    text-decoration: none;
    color: #a28f45;
 }
 .navbar-box li a:hover {
    border-bottom: solid #97772c;
 }
 
 .search-box input {
  padding: 8px 12px;
  border: none;
  border-radius: 6px;
  background: #f3f3f3;
  justify-content: space-between;
}

 .hero {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 60px 80px;
  background: linear-gradient(to right, #abdae7, #eea7f16b);
  margin: 0;
  color: #333;
 }

/* ==== Gambar dan Info ==== */
.image-side {
  position: relative;
}

.butterfly {
  width: 250px;
  border-radius: 53px;
  }

.info-card {
  position: absolute;
  background: #ffffff;
  padding: 8px 14px;
  border-radius: 12px;
  box-shadow: 0 3px 8px rgba(0,0,0,0.1);
  font-size: 14px;
  display: flex;
  align-items: center;
  gap: 6px;
}

.info-card strong {
  color: #333;
  margin-right: 4px;
}

/* posisi tiap kartu */
.info-card.purple {
  top: 20px;
  left: -60px;
}

.info-card.green {
  top: 100px;
  left: 180px;
}

.info-card.bottle {
  bottom: 30px;
  left: 0;
}

/* ==== Text Side ==== */

.text-side {
  max-width: 450px;
}

.text-side h1 {
  font-size: 48px;
  line-height: 1.2;
  color: #2cb189;
}

.text-side h1 span {
  color: #2cb189;
}

.text-side p {
  margin-top: 16px;
  font-size: 16px;
  color: #0a0a0a;
}

nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 50px;
}

.nav-list {
    display: flex;
    list-style: none;
    gap: 28px;
    align-items: center;
}

.nav-list a {
    color: var(--white);
}

button {
  background: #68b1ad;
  border: none;
  padding: 12px 30px;
  border-radius: 10px;
  color: white;
  font-size: 16px;
  cursor: pointer;
  transition: 0.3s;
}

button:hover {
  background: #34928b;
  
}

.product .content > h1 {
text-align: center;
margin-block: 40px;
font-size: 40px;

}

.product {
    width: 100%;
    min-height: 100%;
    margin-bottom: 70px;
    
}
.product-box {
    display: grid;
    grid-template-columns: repeat(3,1fr);
    gap: 20px;
    
}
    .product-box img {
    width: 100%;
    height: 200px;
    border-bottom-left-radius: 10px;
    border-bottom-right-radius: 10px;
    max-width: 100%;
    border-radius: 5px;
    transition: transform 0.3s ease;
  }
  .product-box img:hover {
      transform: scale(1.05);
  }
      

.product-box .desc{
    margin-top: 10px;
    display: flex;
    flex-direction: column;
    gap: 20px;
    background-color: rgb(233, 228, 234);
}
.product-box .desc {
    padding: 20px;
    box-shadow: 0px 0px 15px rgba(66, 125, 149, 0.474);
    text-align: center;
}
.product-box .desc a {
    text-decoration: none;
    color: #471b47;
    background-color: #40b4ac;
    padding: 10px;
    border-radius: 5px;
    transition: all .3s ease;
}
.product-box .desc a:hover {
    background-color: white;
}

/* ABOUT SECTION */
.line {
  background-color: rgba(250, 213, 243, 0.9);
  min-height: 50vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 80px 0;
  margin: 0;
}

.line-container {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 80px;
  width: 85%;
  max-width: 1300px;
  margin: auto;
}


.line-text {
  max-width: 600px;
}

.line-text h1 {
  font-size: 48px;
  color: #279677;
  margin-bottom: 20px;
  font-family: Georgia, 'Times New Roman', Times, serif;
}

.line-text p {
  font-size: 18px;
  line-height: 1.7;
  color: #2f2414;
}

/* CLIENT SECTION */
.client {
  background-color: #f2dbfa;
  padding: 50px 0;
  margin-top: 0;
}

.client_grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 2rem;
  margin-top: 3rem;
  margin-inline: auto;
}

.client_card {
  padding: 2rem;
  background-color: #f7f4f2;
  border-radius: 2rem;
  text-align: center;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
}

.client_card img {
  width: 130px;
  height: 130px;
  object-fit: cover;
  border-radius: 50%;
  margin-bottom: 1rem;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.15);
}

.client_card h2 {
  color: #2cdcd1;
  margin-bottom: 0.5rem;
}

.client_card p {
  color: #2f2414;
  line-height: 1.6;
  font-size: 16px;
}

/* FOOTER */
footer {
  background-color: #dcfffae1;
  color: #11804e;
  padding: 35px 60px;
}

.footer-container {
  display: flex;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 30px;
  border-bottom: 1px solid #080807;
  padding-bottom: 30px;
}

.footer-section h4,
.footer-newsletter h4 {
  font-size: 18px;
  margin-bottom: 15px;
}

.footer-section ul {
  list-style: none;
  padding: 0;
}

.footer-section ul li {
  margin: 8px 0;
}

.footer-section ul li a {
  color: #1e1e1f;
  text-decoration: none;
  transition: 0.3s;
}

.footer-section ul li a:hover {
  color: #141313;
}

.footer-newsletter p {
  color: #090909;
  margin-bottom: 15px;
}

.footer-newsletter form {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
}

.footer-newsletter input {
  padding: 8px 12px;
  border: none;
  width: 200px;
  border-radius: 5px;
}

.footer-newsletter button {
  background-color: #3aab96e7;
  color: #fff;
  border: none;
  padding: 8px 15px;
  border-radius: 5px;
  cursor: pointer;
  transition: 0.3s;
}

.footer-newsletter button:hover {
  background-color: #6bdfbd;
  color: #080807;
  border: none;
  padding: 8px 15px;
  border-radius: 4px;
  cursor: pointer;
}
.footer-newsletter button:hover {
  background-color: white;
}
.footer-bottom {
  text-align: center;
  margin-top: 20px;
  font-size: 14px;
  color: #000;
}